# custom-bazzite-dx

Custom Universal Blue image based on `ghcr.io/ublue-os/bazzite-dx:stable` (KDE, AMD/Intel), published as `ghcr.io/khowe085/bazzite-dx`.

Written 2026-09-11. Decisions marked **(K)** were confirmed by Kevin; **(open)** are my defaults, not confirmed.

## State and next steps (2026-09-25)

Read this first when picking the work up on another machine. Kevin continues on the laptop with a fresh user-level Claude setup, so nothing carries over except this repository: no memory notes, no global instructions, skills or agents from the Windows machine where the work so far was done.

**State.** Everything through PR #7 is merged (`2869451`), and `latest` is built from it. The laptop (Framework 13, AMD Ryzen AI 9 HX 370) was freshly installed from the ISO of run 35962659003, whose image is the `pr-7` build of the same source; Kevin has configured it as he wants. That install follows `pr-7` until it is switched with `sudo bootc switch ghcr.io/khowe085/bazzite-dx:latest`. bazzite-dx is built on Bazzite's Deck (handheld/HTPC) edition, `bazzite-deck`, so Deck-flavoured KDE defaults can turn up that Bazzite's desktop edition does not have; the ones that stay on purpose are listed in the "Deck leftovers" row.

**Kevin's rules for this repo, stated in earlier sessions:**
- **Defaults only (K, 2026-09-23).** Settings ship as image defaults: `/etc/xdg` files, the Add Panel templates and the like. No first-login hooks or Plasma update scripts that rewrite what a profile has already saved. Defaults reach a profile whose first desktop login is on this image, which installing from its ISO provides. The wallpaper update script `picture-of-the-day-default.js` predates this and stays.
- **One branch and one PR (K, 2026-09-20)** for everything not yet merged (`work/claude/<name>`); separate concerns by commits, not branches. Every PR costs a ~25 minute image build.
- **Kevin merges PRs himself on GitHub.** This commit went straight to `main` at his explicit instruction; that was a one-time exception.
- **Commits are signed** (SSH signing through 1Password). If signing fails, ask him; never bypass it. `1Password: failed to fill whole buffer` meant the app was locked.
- **Tests first, then a blind review** by a fresh reviewer (up to 3 rounds) before a PR, as in "Testing (TDD)" below.

**Verifying without a full image build**, as done so far (with podman on the laptop instead of Docker Desktop):
- Every `tests/*.sh` script runs in the base image, for example `podman run --rm -v "$PWD:/src:ro,Z" ghcr.io/ublue-os/bazzite-dx:stable bash /src/tests/test-kde-defaults.sh`, or all of them with `just test ghcr.io/ublue-os/bazzite-dx stable`.
- For `90-verify.sh`, a partial build in a throwaway container of the base: copy `system_files/` to `/` (as `build.sh` does), run the changed build step, then `90-verify.sh`, and read only the sections the change touches; the others fail because the rest of the build did not run.

**Done 2026-09-24 (branch `claude/bazzite-dx-config-sync-61faaf`, not yet a PR): Kevin's panels and desktop settings as defaults.** Read from his live profile on the laptop (`plasma-org.kde.plasma.desktop-appletsrc`, `plasmashellrc`, the live panel properties through plasmashell's `evaluateScript`, read-only) and diffed against the image's defaults; only derived defaults are committed. See the "Desktop config sync" row. Deliberately not shipped: the display scale (1.7, per panel hardware in `kwinoutputconfig.json`), `[Xwayland] Scale` (KWin writes it from that), the colour entries in his `kdeglobals` (Breeze Dark, identical to Fedora Dark's), the kickoff favourites (Bazzite's, unchanged), `kded5rc` and other files written at first login rather than by him.

**Other open items:**
- After 2026-09-29, when Firefox 157 reaches Flathub stable: change `Branch=beta` to `Branch=stable` in `custom-apps.preinstall` before the beta moves to 158 (see the Flatpaks and Firefox rows).
- Building an ISO: start **Build ISO** only after that tag's image build has finished. The ISO copies the image from the registry while it builds, and the first ISO got the previous build that way. Its README section does not say this yet.
- Clean-up on Kevin's side: the `pr-5`, `pr-6` and `pr-7` tags on the GHCR package page (a token needs `read:packages` and `delete:packages`), and the outdated ISO artifact of run 35942041231 (both ISO artifacts expire 2026-12-23).
- Still unverified on the laptop: the Firefox beta ↔ 1Password link, Git commit signing through 1Password (his in-app setup first), `cosign verify` of the image, and `amdgpu.dcfeaturemask=0x402` in `/proc/cmdline`.
- Update size: rebuilds without changes cost about 69 MB since `85-dnf-cleanup.sh` (measured 2026-09-25, `pr-7` to `latest`); the first switch from an older build downloads more.

## Scope

| Item | Approach | Status |
|---|---|---|
| Base image | `bazzite-dx:stable` pinned by digest (Renovate-friendly) | **(K)** |
| .NET 10 SDK | Fedora 44 package `dotnet-sdk-10.0` (10.0.111) | **(open)** vs Microsoft repo |
| 1Password | Official RPM repo, installed at build; relocated `/var/opt/1Password` → `/usr/lib/opt/1Password` + tmpfiles symlink (bazzite-dx's own `/opt` pattern); groups pre-created with fixed GIDs + `sysusers.d` so they exist on existing installs | verified spike |
| 1Password SSH agent + Git signing | **2026-09-20 (K): preconfigure the system per 1Password's docs; Kevin does the in-app part on first boot, and his SSH keys live in 1Password too.** `/etc/ssh/ssh_config.d/60-1password-agent.conf` sets the documented `IdentityAgent ~/.1password/agent.sock` system-wide, but conditionally: `IdentityAgent` outranks `SSH_AUTH_SOCK`, so it applies only when not connected over SSH (1Password documents `Match host * exec "test -z $SSH_TTY"` for that; `SSH_CONNECTION` also covers TTY-less remote sessions, checked against OpenSSH 10.3) and only when the agent socket exists. `git config --system` sets `gpg.format=ssh`, `gpg.ssh.program=/opt/1Password/op-ssh-sign`, `commit.gpgsign=true`; `user.signingkey` stays per user. Not set: `SSH_AUTH_SOCK` for tools that ignore ssh_config, `gpg.ssh.allowedSignersFile` (per-user file), 1Password autostart. | **(K)** |
| Firefox flatpak ↔ 1Password | `xdg-native-messaging-proxy` route: install the proxy, allow-list it in `/etc/1password/custom_allowed_browsers`, oneshot service that (a) copies a pref file (`widget.use-xdg-desktop-portal.native-messaging-proxy=1`) into the `org.mozilla.firefox.systemconfig` extension's `defaults/pref/` (the mechanism Bazzite's own flatpak manager uses for `/usr/share/ublue-os/firefox-config/*.js`) and (b) adds `--talk-name=org.freedesktop.NativeMessagingProxy` to the system flatpak override. Stable Flathub Firefox only gets the proxy client with 157 (2026-09-29 per Mozilla's calendar). **Pivot 2026-09-19 (K): install Firefox beta (157, on beta since 2026-09-14) from the `flathub-beta` remote via the preinstall drop-in; the setup service fills both the `stable` and `beta` extension branches with Bazzite's prefs plus ours.** Flip the drop-in to `Branch=stable` once stable is ≥ 157. | **(K)** |
| EmuDeck | `ujust get-emudeck` is GUI-driven (downloads, then hands the file to Gear Lever), so the first-login hook does its non-interactive equivalent: download the latest EmuDeck AppImage to `~/Applications` + desktop entry. Wizard choices (High integration = Steam ROM Manager, Cloud Sync, Bezels, Autosave, classic 4:3 ARs, Patreon token) cannot be pre-seeded — state lives in Electron localStorage — so they are documented in the README. | **(K)** run Bazzite's recipe; token stays out of image |
| Eden | Fetch latest `Eden-Linux-<ver>-amd64-clang-pgo.AppImage` from git.eden-emu.dev at build into `/usr/lib/eden`; hook places it at `~/Applications/Eden.AppImage` (the exact path EmuDeck's `emuDeckEden.sh` expects, since EmuDeck doesn't download Eden). Replaced when the image carries a newer version than the hook last placed; a user-placed copy (no stamp) is left alone. | variant **(open)** |
| KDE defaults | **2026-09-20 (K): default theme Fedora Dark, background Picture of the Day with the "Astronomy (NASA)" provider.** Both done the way the base does its own. Theme: Bazzite's `steamdeck-kde-presets-desktop` names Vapor in `/etc/xdg/kdeglobals` (`[KDE] LookAndFeelPackage=com.valve.vapor.desktop`); `55-kde-defaults.sh` rewrites that one line to `org.fedoraproject.fedoradark.desktop` (from `plasma-lookandfeel-fedora`, a hard dependency of plasma-workspace) and fails the build if the key is gone. `startplasma` compares the configured package with `~/.config/kdedefaults/package` at every login and regenerates the defaults when they differ, so existing users who never picked a global theme switch too; an explicit choice in `~/.config/kdeglobals` wins. Wallpaper: a Plasma update script next to the base's `bazzite-pins.js` (`/usr/share/plasma/shells/org.kde.plasma.desktop/contents/updates/`); `ShellCorona::load()` runs those once per user, after the default layout for new profiles and at the next login for existing ones. It sets `wallpaperPlugin=org.kde.potd` and `Provider=apod` (the provider's identifier; "Astronomy (NASA)" is its display name) only on desktops still on `org.kde.image` whose `Image` entry is missing (profiles set up under the Fedora themes) or is `/usr/share/wallpapers/convergence.jxl`. **Corrected 2026-09-20 after Kevin's first boot:** I had claimed Vapor writes no `Image` entry, having checked its `defaults` and `layouts` but not `plasmoidsetupscripts/org.kde.plasma.folder.js`, which Bazzite patches to write exactly that path. His profile was created under stock Bazzite before the `bootc switch`, so the first script left his wallpaper alone. The fixed script ships under a new name (`picture-of-the-day-default.js`) because Plasma remembers update scripts by path and had already recorded the old one. My reading of "default": a wallpaper or theme Kevin already chose is not overridden. Lock screen and login screen untouched. **Natural scrolling as the default for mouse and touchpad (K, same day):** the build step appends `[Libinput][Defaults][Touchpad]` and `[Libinput][Defaults][Pointer]` with `NaturalScroll=true` to `/etc/xdg/kcminputrc`. KWin (`Connection::applyDeviceConfig`, present since Plasma 6.3) hands a device the defaults group of its type, and `Device::naturalScrollEnabledByDefault()` reads the key from it; the per-device group System Settings writes to the user's `kcminputrc` wins. Appended rather than shipped as a file because the base may have a `kcminputrc` of its own and KConfig merges repeated groups. | **(K)** theme, wallpaper, natural scrolling for both device types; leaving existing choices alone is **(open)** |
| Flatpaks | Obsidian, Spotify, OBS Studio, Discord (official client, `com.discordapp.Discord`) and Firefox beta via `/usr/share/flatpak/preinstall.d/custom-apps.preinstall` + `custom-flatpak-preinstall.service` (`flatpak preinstall -y` at boot; Fedora ships the command but no unit). bazzite-dx's `/etc/ublue-os/system_flatpaks` has no consumer in the base image, so it cannot be relied on for rebased machines. | **(K)** Spotify/Obsidian/OBS; Kevin chose the official Discord client over Vesktop (2026-09-19) |
| Bazzite Portal selections | **2026-09-20 (K): bake in Kevin's Portal picks; he chose to automate snapshots and dedup too, keeping 5 hourly / 7 daily timeline snapshots and `NUMBER_LIMIT=10`.** I offered that last one to him as "keep 10 manual snapshots", which overstates it: number cleanup only covers snapshots marked for it, and one taken without a cleanup algorithm is kept until deleted. Each entry mirrors its recipe so the Portal's status and toggles keep working, except that the Portal only recognises layered packages and so keeps listing CoolerControl and DisplayLink as not installed. Build time (`65-portal-selections.sh`): `cockpit.service`, `fw-fanctrl.service`, CoolerControl from Terra plus `coolercontrold` (the recipe leaves the daemon off), `displaylink` from negativo17 (the `evdi` kmod is in the base), `/etc/sudoers.d/enable-pwfeedback`, `libvirtd` + `bazzite-libvirtd-setup.service`, `kargs.d` for the two KVM MSR arguments, tmpfiles for `/var/lib/swtpm-localca`. DX's own `virt-on` block never runs because virt-manager is an RPM there, so the kargs and swtpm directory follow the plain Bazzite recipe. First boot: `custom-home-snapshots.service` (config `root` for `/var/home`, as `bazzite-snapper-config` does) and `custom-home-dedup.service` (`configure-beesd`'s sizing, options and ExecCondition for the filesystem behind `/var/home`); both stamp under their `StateDirectory`, leave an existing config alone and finish a half-made one. First login (`35-portal-tweaks.sh`): the FSR4/RDNA3 environment.d file and `systemctl --user enable --now steam-icons-cleanup.service`, once each (not `--global`, which `ujust steam-icons disable` cannot undo), and JetBrains Toolbox through the Portal's brew command in a detached unit. Flatpaks: VacuumTube and `org.jellyfin.JellyfinDesktop` (the recipe's `com.github.iwalton3.jellyfin-media-player` is end-of-life on Flathub and rebases to it). Left manual: Sunshine, Boxtron, Netflix, Crunchyroll and the Steam shortcuts (they need a session or a running Steam). Android Platform Tools are already in DX. SSH stays off, so Cockpit's login (SSH to localhost) does not work until Kevin enables it. **2026-09-20 (K): Cockpit reachable from the local machine only, by firewall rule.** `/usr/lib/firewalld/policies/cockpit-local-only.xml`: a policy from `ANY` to `HOST` with negative priority whose one rich rule rejects tcp/9090. firewalld's INPUT chain accepts `iifname lo` before it dispatches to policies and zones (checked in its nftables backend), so loopback still gets through and no zone (Fedora's workstation zone opens 1025-65535) can re-open the port; Tailscale peers and VMs are refused too. The Bazzite quadlet runs `cockpit-ws` in the host's network namespace via `label-run`, so this is INPUT traffic, not a forwarded container port. Not chosen: binding `cockpit-ws` to 127.0.0.1 through a quadlet drop-in (`Exec=/container/label-run --address=127.0.0.1`), which would not need firewalld but rests on `label-run` passing arguments through, unverified. | **(K)** selection and retention; enabling `coolercontrold` and following the plain recipe for kargs/swtpm are **(open)** |
| Claude Desktop | **2026-09-19 (K): in an Ubuntu distrobox instead of converting the `.deb`.** Manifest `/usr/share/claude-distrobox/claude.ini`, also appended to `/etc/distrobox/apps.ini` (ublue's manifest for `ujust setup-distrobox-app`): image `ghcr.io/ublue-os/ubuntu-toolbox` (Ubuntu 26.04), init hook = `/run/host/usr/libexec/claude-distrobox-init` (Anthropic's apt repo, key fingerprint pinned, `apt install claude-desktop`, skipped once dpkg reports the package installed), app exported to the menu. The first-login hook only launches `/usr/libexec/claude-distrobox-setup` via `systemd-run --user`, because ublue-user-setup runs hooks sequentially; the setup reads the `/usr` manifest (a locally edited `/etc` file survives image updates, and assemble exits 0 when the section is missing), verifies the box exists instead of trusting assemble's exit status, and finishes a half-made box rather than deleting it. | **(K)** automatic at first login |
| Round 2 (2026-09-21) | **(K) one worktree, one PR:** remove CoolerControl; fw-fanctrl; rename the `claude` distrobox to `ubuntu`; LM Studio; Crunchyroll; HDMI 2.1 for AMD; DisplayLink. **Already in the image since PR #4, so unchanged:** fw-fanctrl (no Portal entry exists for it; the package is in the base and `fw-fanctrl.service` is enabled, which is all `ujust enable-framework-fan-control` does) and DisplayLink (`displaylink` from negativo17; the Portal lists it as not installed only because it looks for layered packages). **CoolerControl** and its daemon are out again, leaving the fan to fw-fanctrl; the Terra check in `90-verify.sh` went with it. **Distrobox:** the box is now `ubuntu`, the name the base's `/etc/distrobox/distrobox.ini` gives the same `ubuntu-toolbox` image; only the box name, the manifest section and the stamps (`ubuntu-distrobox.created/.creating`) changed, the `claude-distrobox-*` files keep their names because installing Claude Desktop is still what they do. New stamps mean a machine with the old `claude` box gets the `ubuntu` box as well; the old one is never deleted for him (`distrobox rm claude`). An `ubuntu` box he made himself first counts as his and gets no Claude Desktop. **LM Studio** is the Portal's other brew cask (`lm-studio-linux`), so `jetbrains-toolbox-setup` became `portal-brew-casks-setup`: one detached unit, both casks in sequence, a stamp per cask (`jetbrains-toolbox` kept so his existing install is not repeated), one cask failing does not stop the other, and `--pending` lets the login hook ask whether to start it, so the cask list lives in one place. **Crunchyroll:** `45-crunchyroll.sh` downloads the AppImage the Portal uses (github.com/aarron-lee/crunchyroll-linux, unofficial, no checksums published) to `~/Applications/Crunchyroll.AppImage` with a menu entry, once per user, like the EmuDeck hook; its own late hook so a 110 MB download does not hold up the quick ones, and separate from `30-emudeck.sh` so it cannot break EmuDeck's placement. **HDMI 2.1:** `kargs.d/20-amdgpu-hdmi21.toml` with `amdgpu.dcfeaturemask=0x402`, the argument `ujust configure-amd-hdmi21 enable` sets (the Portal's status reads `/proc/cmdline`, so it shows as enabled); the Portal warns about flicker on some displays. Kevin wrote "HDMI 2.1 Audio"; the Portal's entry is "Enable HDMI 2.1 for AMD graphics cards" and has no separate audio toggle. | **(K)** the list; the readings of fw-fanctrl, DisplayLink and "HDMI 2.1 Audio" are **(open)** |
| More KDE defaults and power (2026-09-22) | **(K) the list:** panels not floating; pointer acceleration off for mice and touchpads ("disable pointer precision"); "Allow briefly lifting finger during tap-and-drag" on touchpads; top-left screen corner "No Action"; "Lock screen automatically: Never"; per-state power profiles (AC: sleep after 30 min, power button sleeps, lid does nothing, dim 15 min, screen off 30 min locked and unlocked, Performance; Battery: 10 / sleep / sleep / 5 / 10, Balanced; Low Battery: 5 / sleep / sleep / 2 / 5, Power Save). **(K) 2026-09-23: defaults only, no hooks or update scripts; he installs from an ISO of this image, so the first desktop login is on it.** (On 2026-09-22 I had added a first-login hook and a panel update script that undid what stock Bazzite saves into a profile at its first login, for installing stock and switching right after; he rejected both.) **(K) the Add Panel templates too.** Read in plasma-workspace, kwin, kscreenlocker, powerdevil and plasma-desktop at `v6.7.4` (the base's Plasma). **Input:** `PointerAccelerationProfile=1` (libinput's flat profile; the KCM's "Enable pointer acceleration" writes it) and `TapDragLock=true` (the touchpad KCM's label for it) join `NaturalScroll=true` in all three `[Libinput][Defaults][<type>]` groups. `Connection::applyDeviceConfig` hands a device its type group *instead of* `[Libinput][Defaults]`, where Bazzite's `/etc/xdg/kcminputrc` already sets the flat profile for every device, so PR #4's natural-scroll groups had turned acceleration back on; the build step now fails if the base sets a key there that the type groups do not repeat. `/etc/skel/.config/kcminputrc` (Bazzite, `[Libinput] PointerAccelerationProfile=2`) is left alone: no Wayland code reads keys directly in `[Libinput]`. **Corner:** `[Effect-overview] BorderActivate=9` in `/etc/xdg/kwinrc`; the kcfg default is `ElectricTopLeft`, and the screen edges KCM saves `ElectricNone` (9) when Overview has no edge. **Lock:** Bazzite's `/etc/xdg/kscreenlockerrc` already has `Autolock=false`, but the KCM shows `Timeout` (default 5), so it read "5 minutes" while the daemon (locks only if `autolock && timeout > 0`) never locked; the step appends `Autolock=false` and `Timeout=0`, what "Never" writes. **Power:** `/etc/xdg/powerdevilrc` (system_files; keys from `PowerDevilProfileSettings.kcfg`, actions 0 = nothing, 1 = sleep, "When locked and unlocked" = `TurnOffDisplayIdleTimeoutWhenLockedSec=-2`, profiles `performance` / `balanced` / `power-saver`). Bazzite (steamdeck-kde-presets) also ships a Plasma 5 `/etc/xdg/powermanagementprofilesrc`; powerdevil's one-time Plasma 6 migration opens that name with the full cascade and saves every value that differs into the new user's `~/.config/powerdevilrc`, which would beat the defaults, so the step removes it (nothing else reads it). A profile first logged into on stock Bazzite has been through that migration already (it also saves `[AC] AutoSuspendAction=0`: Bazzite's AC profile has no suspend group, which the migration reads as never sleep) and keeps those values, hence the ISO. To undo them by hand, `kwriteconfig6 --delete` needs the file's absolute path: on the bare name it writes `Key[$d]`, which hides the system value too (tried). Side effect: `LockBeforeTurnOffDisplay` (hidden; locks before the Turn Off Screen shortcut or button) is powerdevil's default, off, where the migration made it on. **Panels:** a panel's `floating` is saved per panel in `plasmashellrc` (`[PlasmaViews][Panel <id>]`) the first time it is shown, default on, with no global setting; the step inserts `panel.floating = false` after `var panel = new Panel` in every Add Panel template (Fedora's layout loads `defaultPanel`, which Bazzite modifies) and fails if a template makes its panel otherwise or makes a second one; a panel that exists keeps what it saved. | **(K)** list, templates, defaults only with an ISO install; removing the Plasma 5 file is **(open)** |
| Install from an ISO (2026-09-23) | **(K) install from an ISO of this image, so the first desktop login is on it and the defaults apply; (K) no qcow2; (K) Bazzite's own installer.** First try: the template's `build-disk.yml` (bootc-image-builder, qcow2 + `anaconda-iso`), with a `tag` input, `rootfs: btrfs`, per-leg artifact names and `mokutil` in `iso.toml`'s `%post`. Its first run (from the branch) failed on both legs: the ISO while depsolving, because BIB resolves the source image's repos itself and cannot open `terra-mesa`'s `file://` key (`repo_gpgcheck=1`; osbuild/bootc-image-builder#1188, open), and the qcow2 when osbuild's `container-deploy` stage was refused mounting container storage on the runner. BIB is archived (ublue-os/image-template#229). **Now:** Bazzite's live ISO route. `installer/` is Bazzite's `installer/` at `f84dc6e` (Apache-2.0) minus its GNOME files: a container built from `ghcr.io/ublue-os/bazzite:stable` (the live session) that swaps in Fedora's signed kernel so the ISO boots with Secure Boot on, adds `anaconda-live`, the Flathub apps and a stored copy of the image to install, and writes a kickstart: `ostreecontainer` from that copy (`--transport=containers-storage`), then `bootc switch --enforce-container-sigpolicy` to the registry ref, flatpaks, SELinux relabel, and the Secure Boot key queued from `%post --nochroot`. titanoboa (`Zeglius/titanoboa` `revamp-pr`, pinned to `7737f47`, what Bazzite's ISO workflow uses) turns the container into the ISO. Nothing depsolves the image's repos and no osbuild container mount is involved. Checked: the hooks find the image to install with `podman images 'bazzite*'`, which gives `ghcr.io/khowe085/bazzite-dx` and its tag (tried with podman against a placeholder image of that name, and against `ghcr.io/ublue-os/bazzite:stable` as control); the image's `policy.json` rejects by default but accepts any other registry image through the docker transport's `""` scope, so the sigpolicy switch passes (~90%). The installed system follows the tag the ISO was built from. `build-disk.yml` keeps its name (now "Build ISO") because `workflow_dispatch` only runs files that exist on the default branch, which is how a branch's ISO gets built before the merge. `disk_config/iso.toml` and the Justfile's `build-iso`, `rebuild-iso` and `run-vm-iso` went with the old route; the qcow2/raw recipes and `disk.toml` stay for local VMs. | **(K)** |
| Deck leftovers (2026-09-24) | After installing from the ISO, Kevin found a "Return to Gaming Mode" shortcut on his desktop and wanted "Lock after waking from sleep" on. **bazzite-dx is built on `bazzite-deck`** (its build matrix lists only Deck bases; the image name drops "-deck"), so it carries `steamdeck-kde-presets`, whose Deck-only files Bazzite's desktop edition (`steamdeck-kde-presets-desktop`) deletes in its spec: `/etc/skel/Desktop/Return.desktop` (why the shortcut appeared: his earlier profiles were made on the desktop edition), `/etc/xdg/kscreenlockerrc` (`Autolock=false`, `LockOnResume=false`), `kcminputrc`, `powermanagementprofilesrc`, `kdeglobals` (Vapor as in the desktop edition, plus Deck-only `[KScreen] XwaylandClientsScale=false` and 11 pt fonts), the IBus autostart plus `plasma-workspace/env/ibus.sh` (a malformed `export XMODIFIERS= @ im = ibus`), `baloofilerc` (file names only), `kwinrulesrc` (Steam keyboard on top) and `plasma-nm` (`SystemConnectionsByDefault=true`). **(K)** `LockOnResume=true` in the appended `[Daemon]` group (KConfig takes the last value in a file, tried with kreadconfig6); remove the shortcut from skel, the IBus autostart with its env file, and `baloofilerc`; keep `kwinrulesrc` and `plasma-nm`. Review of `1b083f8` pointed out the rest of the Deck `kdeglobals`: **(K)** drop `XwaylandClientsScale=false` (the compositor scaled X11 apps, blurry on his scaled display; KDE's default and the desktop edition let them scale themselves), keep the 11 pt fonts. `kwinrc`'s maliit input method is Deck-only too, but maliit is not installed, so it does nothing. `bazzite-user-setup` only puts the shortcut back on images named "deck", and copies it from the skel file removed here. The shortcut already on his desktop he deletes himself (no profile hooks). | **(K)** |
| Update size (2026-09-24) | Kevin's `bootc upgrade` from the ISO's image to a rebuild of the same source fetched 4 of 128 layers, 871 MB. Compared by layer and by file (the layers are ostree object tars; paths from the commit's dirtrees): the commit metadata (14 MB), `/usr/share/rpm/rpmdb.sqlite` (55 MB layer; install times), dnf5's `transaction_history.sqlite` with its `-wal`/`-shm` (169 MB layer) and `/var/lib/dnf/repos/fedora-…/countme` (12 bytes, riding in the 633 MB layer of unpackaged content and the initramfs). No software differed. **(K)** `85-dnf-cleanup.sh` removes `/var/lib/dnf` (the base has none) and the dnf5 history after every package install, keeping dnf5's package state; rebuilds without changes should then cost about 70 MB (commit metadata and rpmdb, ~80%). The first build with it changes those layers once more. | **(K)** |
| Desktop config sync (2026-09-24) | **(K) his panels go in two new Add Panel templates, not in Bazzite's `defaultPanel`; (K) Plastik as the default window decoration.** Templates `io.github.khowe085.bazzite-dx.topBar` and `.dock` in `system_files`; `55-kde-defaults.sh` replaces Fedora Dark's `loadTemplate("org.kde.plasma.desktop.defaultPanel")` with both (checked first) and floats them off like every template. Top bar: 48 px, full width, system monitors, pager, spacer, tray, then volume/camera/network/Bluetooth/brightness/battery as panel widgets, clock, user switcher, with each widget setting he changed. The tray's `knownItems` lists every tray widget and `extraItems` the ones shown, so the panel widgets are not doubled inside it; written from the script with `reloadConfig()` (read in plasma-workspace 6.7: the tray is a containment, so `config()` is its own group, and `Applet::configChanged` reloads the KConfigLoader that `SystemTraySettings` follows). Dock: 64 px, bottom, centred, autohide, custom length 0.376–0.8235 of the screen width (his 637–1395 of 1694), kickoff with icon `framework`, icontasks with his three launchers (non-empty, so `bazzite-pins.js` leaves them alone), notifications. Decoration: Fedora Dark's `contents/defaults` edited (library `org.kde.kwin.aurorae`, theme `kwin4_decoration_qml_plastik`), because the theme's defaults land in `~/.config/kdedefaults`, which outranks `/etc/xdg`; `startplasma` regenerates kdedefaults only when the package name changes, so it reaches new profiles. Konsole: `DefaultProfile=Vapor.profile` removed from `/etc/xdg/kdeglobals` (built-in profile); paste = `Ctrl+V; Shift+Ins` via `/etc/skel/.local/share/kxmlgui5/konsole/sessionui.rc` holding only `ActionProperties` at `version="1"`: `KXmlGuiVersionHandler` then takes Konsole's newer built-in document, merges the shortcut in and saves it (read in kxmlgui master; a shortcut scheme was rejected because a scheme clears every shortcut it does not list). The comment in that file sits inside `<gui>` because `findVersionNumber` takes the first "gui" and "version" in the text. Notifications: `[Notifications] PopupPosition=TopCenter`, `LowPriorityHistory=true` appended to Plasma's `/etc/xdg/plasmanotifyrc`. The paste shortcut replaces Ctrl+Shift+V, as in his profile, and standalone Konsole then keeps ^V from the programs in it (only the KPart has `overrideShortcutCheck`); noted in the README. Dolphin's Places icon size (22 px in his `dolphinrc`) is not shipped: those `[KFileDialog Settings]` keys are overridden by Dolphin's `[PlacesPanel] IconSize`, whose default is already 22 (found in review). Clock time zones (Los Angeles, Local, London) copied as he has them. Unverified until a fresh profile logs in: the tray config applied from a layout script, and `panel.lengthMode = "custom"` with lengths set before the view exists. The templates were smoke-run in node against stub `Panel`/widget objects. | **(K)** templates, Plastik |
| 1Password in the distrobox (2026-09-24) | **(K) "punch through" the host's SSH agent and commit signing to the `ubuntu` box**, asked when commits from the box (where Claude Code runs) could not be signed. `claude-distrobox-init` (the box's init hook, read from `/run/host` on every start, so image updates reach existing boxes) now does, before its installed-package early exit: link `/opt/1Password` to `/run/host/usr/lib/opt/1Password` (the host's `/opt/1Password` leads through the absolute link `/var/opt/1Password -> /usr/lib/opt/1Password`, which would resolve inside the box; a real directory there is left alone), mirror `gpg.format`, `gpg.ssh.program` and `commit.gpgsign` from the host's `/etc/gitconfig` into the box's (`--replace-all`, and `--unset-all` for a key the host no longer sets), and install the host's `60-1password-agent.conf` into the box's `ssh_config.d` (Ubuntu's `ssh_config` includes it). The home directory, agent socket and `~/.gitconfig` are shared already. Distrobox's entrypoint runs init hooks under errexit and does not start the box when one fails, so every step of this block only warns (review finding; a stale `gitconfig.lock` or a key added twice made `git config` fail). Side effect, as on the host: commits in the box need `user.signingkey` first. `op-ssh-sign` needs only glibc and runs in Ubuntu 26.04 (checked); the hook was run once in Kevin's box. Still per user: the app's SSH agent and `user.signingkey`. | **(K)** |
| gh, tea and Lua in the distrobox (2026-09-24) | **(K) gh and tea in the box with their config punched through; lua5.1, lua5.1-dev, luarocks5.1, luacheck, and busted from LuaRocks 5.1.** gh and tea: the hook links the host's `/usr/bin/gh` (2.97, Fedora's build, runs on Ubuntu's glibc) and `/usr/bin/tea` (static) to the same box paths, like 1Password: same versions as the image, no download, updated with it. Ubuntu 26.04's `gh` is 2.46 and its `tea` is an unrelated program. `/usr/bin` because `gh auth setup-git` writes `!/usr/bin/gh auth git-credential` into the shared `~/.gitconfig`; that helper was missing in the box, so HTTPS pushes from it failed. gh's token is in the desktop keyring, which the box reaches over the shared session bus (checked: `gh auth status` in the box); `~/.config/gh` and `~/.config/tea` are in the shared home. A real file at either path (a box-installed package) is left alone. Lua: Kevin's names map to Ubuntu's `lua5.1`, `liblua5.1-0-dev` (there is no `lua5.1-dev`), `luarocks` (one package for all Lua versions; `luarocks5.1` is not a package, `--lua-version 5.1` selects it) and `lua-check` (luacheck 1.2.0); `build-essential` added because busted's dependencies (luafilesystem, luasystem, lua-term) are C modules and the box has no compiler. busted via `luarocks --lua-version 5.1 install busted` into `/usr/local`. The hook's early exit now requires Claude Desktop, those packages and busted (`luarocks show`, offline); missing ones are installed on the next start, so existing boxes get them. Claude Desktop keeps failing the hook on error as before; the Lua tools only warn, so an offline start still starts the box. Run once in Kevin's box: all tools present, a busted spec passes under lua5.1, luacheck runs, git reaches GitHub through the linked gh, later starts take 0.1 s. | **(K)** list; package-name mapping and build-essential **(open)** |
| Offline first boot (2026-09-25) | Kevin's first boot of a `pr-8` ISO install: the Wi-Fi profile only exists once plasma-setup creates it (uid 968, 48 s into boot), so `custom-flatpak-preinstall.service` (21 s) found no network and printed "Nothing to do" with exit 0, and `cockpit-container.service` failed its image pull five times in a second and hit the start limit. The login hooks were fine: the Wi-Fi was up by then. **(K) the image's flatpaks go on the ISO:** `installer/install-image-flatpaks.sh`, a second `RUN` in `installer/Containerfile` (Bazzite's `build.sh` stays unchanged), adds flathub-beta and runs `flatpak preinstall` against the image's `preinstall.d`, which the workflow passes as the `image-files` build context from the checkout; `install-flatpaks.ks` then rsyncs `/var/lib/flatpak` with the preinstalled marks. The boot service stays for rebased machines and list changes. Flatpaks update through `uupd`, not the image. **(K) EmuDeck and Crunchyroll in the image like Eden, not on the ISO** (the user does not exist when the ISO is built, and the ISO would freeze their version): `build_files/fetch-appimages.sh` (no `NN-` prefix: the Containerfile runs it in a `RUN` of its own, the only one given the workflow's `GITHUB_TOKEN` as a build secret, because that token can push packages and the other steps run third-party scriptlets; the token only lifts the API rate limit runners share, and goes to curl through a `-K` file so xtrace never shows it) ships the latest releases to `/usr/lib/{emudeck,crunchyroll}` with `VERSION` (Crunchyroll checked against GitHub's asset digest; EmuDeck's assets have none); both hooks call `/usr/libexec/image-appimage`, which places the file, replaces it whenever the image version differs but only while its sha256 still matches what it placed (EmuDeck updates itself), never puts back a moved or deleted file, respects a user's own copy, and takes over the earlier hooks' `.<id>.image-placed` downloads only when byte-identical to the image's copy (Crunchyroll's stamp names the asset, but the file was saved as `Crunchyroll.AppImage`, which is checked first). Two blind review rounds; the second's remaining low items: `flatpak preinstall` in the live image would also install any preinstall lists Bazzite's live image ships (none seen on the installed system). Cockpit's retry is not addressed yet (base quadlet). | **(K)** |
| Tailscale | Already in base (RPM 1.102.3). `tailscaled` enabled at build = what `ujust tailscale enable` does. | **(K)** |
| 1Password CLI | Not included. | **(open)** |
| `gh` and `tea` | **2026-09-20 (K).** `gh` from Fedora 44's package (2.97.0; the base's convention is to avoid extra repos). `tea` = Gitea's CLI (my reading of "tea"; it also works with Forgejo): nobody packages it, so `build_files/install-tea.sh` takes the latest release binary from dl.gitea.com, version found through the `releases/latest` redirect, verified against the published `.sha256`, installed to `/usr/bin/tea`. | **(K)** include both; sources **(open)** |

## Repo layout (from ublue-os/image-template)

```
Containerfile                 FROM bazzite-dx:stable@sha256:…, runs /ctx/build.sh, bootc container lint
Justfile, image-template.env  template recipes; IMAGE_NAME=bazzite-dx, REPO_ORGANIZATION=khowe085
.github/workflows/            build.yml (build+push+cosign sign), build-disk.yml ("Build ISO": Bazzite's live installer from installer/, via titanoboa)
installer/                    Bazzite's installer/ at f84dc6e, GNOME files left out (see installer/README.md)
build_files/build.sh          copies system_files/ (modes normalised), runs NN-*.sh in order
build_files/10-dotnet.sh
build_files/20-1password.sh
build_files/25-1password-git-signing.sh   system git config for SSH signing through op-ssh-sign
build_files/30-firefox-native-messaging.sh   installs xdg-native-messaging-proxy, enables service
build_files/40-eden.sh        downloads the Eden AppImage into /usr/lib/eden + VERSION stamp
build_files/50-tailscale.sh   enables tailscaled
build_files/55-kde-defaults.sh   Fedora Dark as the default global theme and X11 apps scaling themselves (/etc/xdg/kdeglobals), input defaults (/etc/xdg/kcminputrc), top-left corner (/etc/xdg/kwinrc), no automatic lock but lock after waking from sleep (/etc/xdg/kscreenlockerrc), removes the Plasma 5 power profiles and Deck-only files (Return to Gaming Mode shortcut, IBus autostart and env, baloofilerc), non-floating Add Panel templates, notification popup position, Konsole's built-in profile, Fedora Dark's decoration (Plastik) and layout (the image's two panels)
build_files/60-flatpaks.sh    enables custom-flatpak-preinstall.service
build_files/65-portal-selections.sh   Bazzite Portal picks: enables units, installs DisplayLink
build_files/70-claude-distrobox.sh   appends /usr/share/claude-distrobox/claude.ini to /etc/distrobox/apps.ini
build_files/80-forge-clis.sh  installs gh (dnf) and runs install-tea.sh
build_files/85-dnf-cleanup.sh  removes the bookkeeping the build's dnf runs leave (/var/lib/dnf, dnf5 install history), so rebuilds do not change large layers
build_files/install-tea.sh    downloads and checksum-verifies Gitea's tea; not numbered, so build.sh does not run it on its own
build_files/90-verify.sh      image test (see Testing)
system_files/etc/1password/custom_allowed_browsers
system_files/etc/xdg/powerdevilrc   power management defaults per power state
system_files/etc/skel/.local/share/kxmlgui5/konsole/sessionui.rc   Konsole's Ctrl+V paste, merged by KXmlGui into Konsole's own menus
system_files/etc/ssh/ssh_config.d/60-1password-agent.conf   every host uses 1Password's SSH agent socket
system_files/etc/flatpak/remotes.d/flathub-beta.flatpakrepo   Flathub's official beta remote file, verbatim
system_files/etc/sudoers.d/enable-pwfeedback   what `ujust password-feedback on` writes
system_files/usr/lib/bootc/kargs.d/10-kvm-msrs.toml   kernel arguments of `ujust setup-virtualization virt-on`
system_files/usr/lib/bootc/kargs.d/20-amdgpu-hdmi21.toml   kernel argument of `ujust configure-amd-hdmi21 enable`
system_files/usr/lib/firewalld/policies/cockpit-local-only.xml   rejects Cockpit's port from every zone; loopback is accepted before policies
system_files/usr/lib/sysusers.d/onepassword.conf
system_files/usr/lib/tmpfiles.d/onepassword.conf
system_files/usr/lib/tmpfiles.d/swtpm-localca.conf
system_files/usr/lib/systemd/system/onepassword-firefox-flatpak.service
system_files/usr/lib/systemd/system/custom-flatpak-preinstall.service
system_files/usr/lib/systemd/system/custom-home-snapshots.service, custom-home-dedup.service   first-boot oneshots
system_files/usr/libexec/onepassword-firefox-flatpak-setup
system_files/usr/libexec/claude-distrobox-init   runs inside the ubuntu box as its init hook: 1Password signing/agent from the host, then Claude Desktop
system_files/usr/libexec/claude-distrobox-setup  per-user creation of the box, started detached by the hook
system_files/usr/libexec/custom-home-snapshots-setup   Snapper config for /var/home, once
system_files/usr/libexec/custom-home-dedup-setup       beesd config for the filesystem behind /var/home, once
system_files/usr/libexec/portal-brew-casks-setup       per-user brew cask installs (JetBrains Toolbox, LM Studio), started detached by the hook
system_files/usr/share/claude-distrobox/claude.ini   the box's manifest
system_files/usr/share/ublue-os/firefox-config/zz-onepassword-native-messaging.js
system_files/usr/share/plasma/layout-templates/io.github.khowe085.bazzite-dx.{topBar,dock}/   the two panels of a new profile, also in Add Panel
system_files/usr/share/plasma/shells/org.kde.plasma.desktop/contents/updates/picture-of-the-day-default.js   Plasma update script, once per user
system_files/usr/share/flatpak/preinstall.d/custom-apps.preinstall
system_files/usr/share/ublue-os/user-setup.hooks.d/30-emudeck.sh
system_files/usr/share/ublue-os/user-setup.hooks.d/35-portal-tweaks.sh
system_files/usr/share/ublue-os/user-setup.hooks.d/45-crunchyroll.sh   Crunchyroll AppImage into ~/Applications, once per user
system_files/usr/share/ublue-os/user-setup.hooks.d/40-claude-distrobox.sh
tests/                        script tests (bash, run in a container via `just test`)
```

## Testing (TDD)

- `build_files/90-verify.sh` is written first and run against the untouched base (red), then runs as the last build step (green). It asserts package presence, file modes/ownership (setgid `1Password-BrowserSupport`, setuid `chrome-sandbox`), fixed GIDs, tmpfiles/sysusers entries, allow-list contents, service enablement, the Firefox pref file, the beta remote and its signing key, AppImage + stamp, hook syntax.
- `tests/test-1password-ssh-git.sh` runs the git-signing build step against a scratch system gitconfig (values set, existing settings kept, idempotent, plain commit refused until a key exists) and feeds the ssh drop-in to the real `ssh -G`.
- `tests/test-claude-distrobox-init.sh`, `-setup.sh` and `-hook.sh` cover the in-box install script (stub curl/gpg/apt-get/dpkg-query), the per-user setup (stub distrobox) and the launcher hook (stub systemd-run).
- `tests/test-install-tea.sh` covers the tea download (stub curl, real sha256sum): happy path, checksum mismatch, unusable version lookup, failed download.
- `tests/test-emudeck-hook.sh` and `tests/test-firefox-setup.sh` run the two runtime scripts with a temp `HOME`, a stub `flatpak`, and overridable source dirs, asserting the files they produce.
- `tests/test-home-snapshots-setup.sh` and `test-home-dedup-setup.sh` run the two first-boot setups with stub findmnt/snapper/systemctl and a scratch root: first boot, later boots, non-btrfs, an existing user config, each failure point and the retry after it, the bees sample under /usr/etc or /etc, the bees sizing table. `tests/test-portal-tweaks-hook.sh` and `test-portal-brew-casks-setup.sh` cover the first-login hook (stub systemctl/systemd-run, the real setup script for its `--pending` question) and the brew installs (a stub brew that is only on PATH once `shellenv` has been eval'd; both casks, one already stamped, one failing, `--pending`). `tests/test-crunchyroll-hook.sh` mirrors the EmuDeck hook test (stub curl with a fake release and AppImage). Both AppImage hook tests need `jq`, which Git Bash lacks; locally they were smoke-run with a throwaway PowerShell stand-in for the one jq expression, where only the checks that need real symlinks or a non-shebang executable bit fail. `90-verify.sh` also guards what these copy from the base: the recipe's constants and options, the Portal's brew command, the FSR4 file name, the units and the bees sample.
- Local runs use Docker Desktop (`docker build` / `docker run`); CI uses the template's podman recipes.
- `tests/test-kde-defaults.sh` runs the KDE build step against scratch copies of Bazzite's kdeglobals (one line changed, fails when the key or the file is gone; in a separate case the Deck's `XwaylandClientsScale` line is dropped, so the main case still sees a single changed line), kcminputrc (the three type groups appended, with or without an existing file, with Bazzite's real one, and failing when the base sets a key for every device that the groups do not repeat), kwinrc, kscreenlockerrc (also without Bazzite's `Autolock=false`; `LockOnResume=true` wins over its `false`), the four Deck-only files (removed, or already gone), the Plasma 5 power profiles (removed, or already gone) and the three Add Panel templates (one line inserted each; fails, changing none, when one makes its panel another way or a second panel, or none are there). Values are read back with `kreadconfig6`. **2026-09-22 (K):** the three natural-scrolling checks that asserted each type group held `NaturalScroll=true` alone became one check per key. The wallpaper update script is JavaScript for Plasma's scripting engine and the repo has no JS runtime to test with: its logic ran once against stub desktops under Windows Script Host (declarations rewritten to `var`), and `90-verify.sh` checks statically that it, the Fedora Dark theme, the `org.kde.potd` wallpaper and the APOD provider plugin are in the image. `90-verify.sh` checks the Add Panel templates statically.
- `tests/test-dnf-cleanup.sh` runs `85-dnf-cleanup.sh` against a scratch root: `/var/lib/dnf` and dnf5's `transaction_history.sqlite*` go, `packages.toml`, `nevras.toml` and `system.toml` stay, a second run with nothing to remove succeeds. `90-verify.sh` checks the same in the image.
- Review gate: `code-reviewer` agent, up to 3 rounds, before anything is pushed. No commits without Kevin's go-ahead.

## Kevin's manual steps

1. ~~Create the GitHub repo `khowe085/bazzite-dx`~~ Done 2026-09-19 (public). Nothing pushed yet.
2. ~~Generate the cosign key pair, add `cosign.key` as repo secret `SIGNING_SECRET`, commit `cosign.pub`~~ Done 2026-09-19 (empty password, as the workflow expects). `cosign.key` stays local and gitignored.
3. ~~Push; first build publishes `ghcr.io/khowe085/bazzite-dx:latest`.~~ Done 2026-09-19.
4. ~~On the machine: `sudo bootc switch ghcr.io/khowe085/bazzite-dx:latest`, reboot.~~ Replaced 2026-09-24 by installing from the ISO (README, "Installing from an ISO"); that install then needs the switch to `latest`.
5. Run the EmuDeck wizard once (choices listed in README); enter the Patreon token there.
6. 1Password in-app setup: SSH agent and commit signing (README, "SSH keys and Git signing with 1Password").

## Risks / uncertainty

- The 2026-09-19 Firefox-beta pivot was not built or run locally (Kevin chose to skip Docker runs): only the stubbed script test ran, in Git Bash. Unverified flatpak behaviour it relies on: `flatpak preinstall` installing `Branch=beta` next to an already-installed stable, the extension point following the app's branch, and the newly installed branch becoming current. The first CI build is the first real run of `90-verify.sh` with these changes. **Confirmed on Kevin's laptop 2026-09-20** (fresh stock install, then `bootc switch`): the preinstall service installed Obsidian, Spotify and Firefox beta (157.0b3) next to the stable Firefox, both remotes are configured, and the launcher opens the beta. It takes a while after the first boot, during which Firefox still shows the stable version. Still unchecked there: the systemconfig prefs on the beta branch and the 1Password link itself.
- Claude Desktop in a distrobox is untested on a real desktop: Electron sandbox in a rootless container, browser login hand-off (`claude://`), Cowork (QEMU/KVM in the box). Mechanics confirmed only by reading distrobox 1.8.2.5's source (the version Fedora 44 ships).
- KDE defaults have not run under Plasma: the update script's first real run is a login on Kevin's machine. Mechanics confirmed by reading plasma-workspace (`startplasma.cpp`, `shellcorona.cpp`, `scriptengine.cpp`) and kdeplasma-addons on their Plasma/6.7 branches, not by running them. The image wallpaper only writes its `Image` entry when the user picks or drops a picture (its `main.xml` default is empty, and `main.qml` assigns it in the drag-and-drop handler alone), which is what the script's "untouched" test rests on; if that reading is wrong, the script leaves his wallpaper alone and he sets Picture of the Day by hand.
- The 2026-09-22 KDE and power defaults have not run under Plasma either; the build step and the verify checks ran in a container of the pinned base (`system_files` copied, step 55 run, then `90-verify.sh`'s KDE and power sections), not in a built image. Unverified on a machine: powerdevil taking its defaults from `/etc/xdg/powerdevilrc` (its settings class opens `powerdevilrc` with the full cascade, ~90%), `panel.floating` set from a template before the panel's view exists (it then writes `plasmashellrc` directly), and the touchpad accepting the flat profile (libinput 1.31 offers it for touchpads, ~85%).
- The ISO route: run 35942041231 (`3baa272`, tag `pr-7`) built the ISO in 30 minutes (`iso` artifact, 10.06 GiB, expires 2026-12-23), and its log shows the hooks resolving `ghcr.io/khowe085/bazzite-dx:pr-7`. The first run (35941682324) was cancelled: review found that titanoboa resolves `iso-dest` from its own directory and needs every directory in it to exist (replayed with its exact lines), so the workflow passes a bare file name and moves the ISO afterwards, as Bazzite does. `LIVE_IMAGE` is Bazzite's `:stable` by tag while the installer copy is pinned, so a Bazzite change the copy does not know about can break the build until the copy is refreshed. The workflow fails early if the image name does not start with `bazzite`, which the installer's lookup needs. Unverified: the install and first login on the laptop (the installer's nvidia and deck branches key on the name; this image takes the plain KDE path), and the HDMI 2.1 kernel argument reaching the installed system (review: bootc's install step applies `kargs.d`, ~80%).
- Firefox proxy route is untested end to end; 1Password accepting `xdg-native-messaging-proxy` as the parent process is inferred from the `xdg-desktop-portal` / `flatpak-session-helper` precedents (~75%).
- Fixed GIDs (see sysusers.d) must stay free on the target machine; sysusers falls back to another GID on collision, which would silently break the setgid check.
- 1Password's polkit owner list is generated from `/etc/passwd`, which is empty at image build time, so the build bakes in `unix-user:1000` … `unix-user:1009`. Users with other UIDs cannot use the CLI/SSH-agent authorisation prompts.
- The Portal selections (2026-09-20) have not run on a machine. First exercised by CI: `displaylink` resolving against the base's `evdi` kmod, the Terra install, unit names. First exercised on Kevin's laptop: `bootc` applying `kargs.d` on Bazzite, snapperd answering a boot-time unit under SELinux, `brew trust` and the cask install from a systemd user unit, Cockpit's SSH-based login.
- Review of `7a6db9d` + `1ad434a` (2026-09-20, after the merge): ISSUES. CI had passed every `90-verify.sh` section and all 12 tests on `1ad434a`. Its one blocking finding, that the wallpaper update script has no behavioural test, is **waived by Kevin (2026-09-20): no wallpaper test needed.** The image has no JavaScript engine to run one on anyway (Fedora ships Qt's `qml` tool in `qt6-qtdeclarative-devel`; the base installs no node, gjs or quickjs). Taken up: natural scrolling also for keyboards with a built-in pointing device (KWin gives those the `Keyboard` defaults group only), the bees 30-minute guard now checks `bees-timeout.conf` (the per-filesystem `override.conf` masks the base's file of that name), and the in-progress marker is removed before the stamp is written. Left as is: the dedup setup sizes the bees hash table from `findmnt`'s statvfs size where the recipe uses `lsblk`'s device size; same result on a single-device filesystem such as Kevin's, a smaller table on multi-device or RAID1 btrfs.
