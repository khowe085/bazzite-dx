// Let the 1Password extension reach the host 1Password app through xdg-native-messaging-proxy
// (Firefox 157 and later). 0 = never, 1 = always, 2 = auto-detect the sandbox.
pref("widget.use-xdg-desktop-portal.native-messaging-proxy", 1);
