# The swtpm package owns this directory, but an image does not ship /var, so emulated TPMs (Windows
# 11 guests) fail without it. `ujust setup-virtualization virt-on` makes it by hand.
d /var/lib/swtpm-localca 0750 tss root -
