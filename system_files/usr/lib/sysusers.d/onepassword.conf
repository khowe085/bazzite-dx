# 1Password-BrowserSupport and 1password-mcp are setgid to these groups in the image, and bootc never
# merges /etc/group, so the groups must be recreated by number on every machine at boot.
g onepassword     20200
g onepassword-mcp 20201
